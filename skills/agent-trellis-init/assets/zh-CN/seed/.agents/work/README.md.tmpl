# Active Work

只有任务跨会话、跨阶段或需要明确交接时，Active Work Item 才保存临时执行状态。

## 生命周期

1. 只有连续性需要时，才创建 `.agents/work/active/YYYY-MM-DD-short-title.md`。
2. 记录范围、当前状态、证据、下一项安全动作、阻塞项和受影响工作树。
3. 在有意义的交接点更新；不得将其用作命令流水账。
4. 完成时，将长期决策迁移到 Agent Notes，将当前事实迁移到 owning 文档。
5. 将完成项归档到 `.agents/work/archive/`；临时证据不再有用时可以移除。

Active Work 永不拥有架构或政策。
