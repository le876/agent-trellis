[English](AGENTS.md)

# 仓库规则

本仓库分发 Agent Trellis，不拥有任何被初始化仓库的项目事实。

## 事实归属

- `README.md` 拥有公开产品说明和安装入口。
- `skills/agent-trellis-init/SKILL.md` 拥有初始化器行为。
- Skill 的 references 分别拥有发现、生成和验证细节。
- 英文文档和英文模板是 canonical 版本。中文文件是由 `i18n/pairs.json` 声明的完整同步译本。
- `THIRD_PARTY_NOTICES.md` 拥有第三方来源信息。

每项事实只设置一个 owner，其他位置通过链接引用。

## 修改规则

- 同一项变更必须同步更新两种语言。每个已声明配对须保持标题结构和占位符一致。
- 公开历史和可分发资源不得包含目标仓库的名称、路径、远端、架构、提交标识或实现事实。
- 模板可以包含明确的双花括号字段；已发布文档不得包含占位符。
- 不要向核心输出加入框架专用默认值。只有目标仓库提供证据时，才添加条件 owner。
- 初始化器必须保留用户已有规则和未提交修改，并在写入前取得明确确认。

## 验证

提交或推送前运行 `python3 scripts/validate.py`。只报告实际运行过的检查。除非被初始化仓库的用户另行授权，否则不得替其提交、推送或执行会改变外部状态的操作。
